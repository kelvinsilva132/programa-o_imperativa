//EQUIPE 04//
//CAUE
//FELIPE
//DIEGO

//-FIZZ BUZZ-//

let fizzBuzz = (num1, num2) => {
    for (let i= 1; i<= 100; i++) {
       if(i % num1 == 0) {
          console.log(`${i} - ${i % num2 == 0 ? 'FizzBuzz' : 'fizz'}`);
       
} else if (i % num2 == 0) {
           console.log(`${i}} - Buzz`);

       } else {
console.log(`${i} - Nem ${FizzBuzz} e nem ${fizz}`)
       }
    
   }

}
FizzBuzz(2 ,3)

//2

let fatorial = (num1) => {
    let fat = 1;

    if (num === 0){
       
        return console.log(fat);

    } else {

        for (let i = num; i > 1 ; i--){
            fat *= i;
        }

        return console.log(fat);
    } 

    let multFat = (mem, result) => {
      let result, mem = num;
    if (nem > 1){

    };
}
    
    }

    function fatorial1(num){
         if(num === 0){ return console.log(fat); 
        } else { for(let i = num; i > 1 ; i--){ fat *= i; } return console.log(fat); } } 
let fat = 1; 
function fatorial(n){
     if (n === 0){ } else 
     { fatotal(n)*fatorial(n-1) } } 